# 0015 — cowork — Final repository audit

**When:** 2026-08-03T04:10Z
**Agent:** cowork
**Task:** final audit
**Findings touched:** F-026, F-027 (opened)
**Files changed:** this entry, `audit/findings/FINDINGS.md`

## What changed
Audited `origin/main` as a reviewer would see it, not the working tree.
Working tree is clean, local and remote are level at `d01b103`, and all 25
prior findings are closed. Two new findings opened, one of them blocking.

## Verified how

Committed tree (`git ls-tree -r origin/main`) contains everything needed to
score and run the work:

- `lib/` 11 modules, `app/api/` 10 routes across 5 resources
- `scripts/{schema.sql,seed-db.ts,demo.ts,test-tax.ts}`
- `data/{tax-rules.json,transactions.json}`; the generated `.db` is correctly
  absent and regenerated by `db:seed`
- `reports/compliance-report-{AR,BR,CL,CO,PE}.json`, each with a `disclaimer`
  key and the required `edgeCases` block
- `README.md` 266 lines: business framing, disclaimer, quick start, endpoint
  table, worked curl for calculate / rate-change / audit / report, tax rules,
  performance, architecture, Vercel trade-off
- `ARCHITECTURE.md` 334 words, inside the required 200-400
- `verify/smoke-test.sh`, `docs/` 12 files, `audit/` 16 log entries
- `scripts/test-tax.ts`: 559 lines, 19 cases, 88 assertions, covering both time
  axes, B2B reverse charge, thresholds, CLP zero-decimal, NO_APPLICABLE_RULE,
  and the append-only trigger rejecting UPDATE

API verb surface confirmed complete:
```
POST   /api/tax/calculate
GET    /api/tax/rules            POST /api/tax/rules
GET    /api/tax/rules/[ruleKey]  PUT  /api/tax/rules/[ruleKey]  DELETE same
GET    /api/tax/rules/[ruleKey]/versions
GET    /api/tax/report
GET    /api/audit  /api/audit/[id]  /api/audit/[id]/replay
GET    /api/health
```

Findings registry: zero rows remain OPEN.

## For the next agent
Two things left, in `audit/findings/FINDINGS.md` as F-026 and F-027. F-026 is
blocking: the production URL returns 404 and it is the submission's Deliverable
URL. Could not diagnose from here; the Vercel MCP available to this agent
authenticates to a different team and returns 403 on
`team_iZtrUMgQy7fcyShR98St3elu`.
