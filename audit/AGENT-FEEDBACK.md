# AGENT-FEEDBACK — SUPERSEDED

The rubric audit agent's memory is now:

- `audit/scorecard/LATEST.md` — previous pass's per-criterion score and where to
  start next (read at the start of every pass)
- `docs/12-RUBRIC-AUDIT-AGENT.md` section 9 — the living do-not-re-raise list

The hourly regression agent's memory is `audit/REGRESSION-STATE.md`, per
`docs/13-REGRESSION-AGENT.md`.

Do not use this file. It is kept only so the merge is traceable. An unreferenced
memory file is a stale-claims generator, which is the exact failure this
repository has already shipped twice (`NOTES.md`, `ARCHITECTURE.md`).
